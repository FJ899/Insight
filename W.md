# W

Append-only capture file for command `W`.

Rule: when Human sends `W`, append the immediately preceding Assistant response verbatim, with timestamp and minimal source context. If Human sends `W: <text>`, append exactly the supplied text instead.

No summarization. No classification. No reinterpretation.

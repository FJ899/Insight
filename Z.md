# Z

Append-only capture file for command `Z`.

Rule: when Human sends `Z`, append the immediately preceding Assistant response verbatim, with timestamp and minimal source context. If Human sends `Z: <text>`, append exactly the supplied text instead.

No summarization. No classification. No reinterpretation.
